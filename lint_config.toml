base = "ruff"
config = "ruff.toml"

[lint]
command = "ruff check"
config = "ruff.toml"
paths = ["src", "tests", "scripts"]

[format]
command = "ruff format"
config = "ruff.toml"

[test]
command = "pytest"
config = "pytest.ini"

