# Core analytics stack (inherits base runtime dependencies)
-r requirements.txt

# Optional scientific/feature methods used in some stages
dcor>=0.6
numba>=0.56,<0.61
iisignature>=0.24

# Optional integrations (install on demand)
# mlflow>=2.7
# RL stack is provided by orchestrator stages when needed
# stable-baselines3==2.1.0
# sb3-contrib==2.1.0
# torch>=2.1,<2.7
