# Reports

The active reports directory is intentionally empty. Previously generated
campaign materials from 2025 now live under
`archive/2025-10-17-research-report/reports/`.

Running `leadlag.reporting.generate_report` will populate this directory with
the latest outputs.

