scheme: walk_forward_purged
n_splits: 6
embargo_frac: 0.01
embargo:
  frac: ${..embargo_frac}
manifest:
  persist: true
  filename: data_splits.json
nested_tuning:
  enabled: true
  inner_n_splits: 3
  inner_embargo_frac: ${..embargo_frac}
