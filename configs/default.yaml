defaults:
  - scenario: fixed_30
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

output_root: results

multi_seed:
  enabled: true
  seeds: [42, 52, 62]

