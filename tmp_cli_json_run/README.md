# CLI Run Scratch Space

This directory is reserved for temporary outputs produced by CLI smoke tests and
debug runs. The historical artifacts that previously lived here were archived
under `archive/2025-10-22-cli-dry-run/tmp_cli_json_run/`.

Fresh runs may create new files here; clean up the directory before committing
or archive notable outputs with a dated folder.

