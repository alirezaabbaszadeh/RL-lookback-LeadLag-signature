[pytest]
addopts = -ra
minversion = 7.0
testpaths = tests
markers =
    slow: mark tests that take longer or use heavier resources
    e2e: mark end-to-end integration tests
    kaggle: mark tests requiring Kaggle-specific dependencies or environment
